name: Android Build
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  checkout-and-stash:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Look
        run: ls -al

  unstash:
    runs-on: ubuntu-latest
    needs: [checkout-and-stash]
    steps:
      - name: Check
        run: ls -al
